[package]
name = "tools"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
frontend = { path = "../frontend" }
semantics = { path = "../semantics" }
ir = { path = "../ir" }
